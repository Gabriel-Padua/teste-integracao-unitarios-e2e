info:
  name: buscar-autor-por-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/autores/:id"
  params:
    - name: id
      value: "6"
      type: path
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
